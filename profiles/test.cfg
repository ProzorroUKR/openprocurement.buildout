[buildout]
extends = dp-dev.cfg
eggs -= ExtendedJournalHandler

[openprocurement.api.ini]
journal =

[openprocurement.concord]
eggs -= systemd-python


[scripts]
eggs +=
    WebTest
    mock
    pytest
scripts +=
    py.test